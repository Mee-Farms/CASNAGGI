[build]
  base = "frontend"
  command = "npm run build"
  publish = "build"

# This ensures React Router handles all URLs without 404 errors
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
